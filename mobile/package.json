{
  "name": "kelaska-mobile",
  "version": "1.0.0",
  "description": "KelasKA Mobile App Wrapper (Capacitor)",
  "private": true,
  "scripts": {
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
